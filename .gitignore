# Node
node_modules/
npm-debug.log*
*.tsbuildinfo
dist

# Rust
target/
**/*.rs.bk

# env / secrets
.env
.env.*
!.env.example
keys/
*.pem

# macOS
.DS_Store

# IDE
.idea/
.vscode/

# Local runtime state
.data/
data/

# spec-spine derived output: committed shards only
.derived/spec-registry/*
!.derived/spec-registry/by-spec/
.derived/codebase-index/*
!.derived/codebase-index/by-spec/
!.derived/codebase-index/by-package/
!.derived/codebase-index/config-hash.json
!.derived/codebase-index/slices.json

# Claude Code local state
.claude/settings.local.json
.claude/agent-memory/

.worktrees/

# The members' runtime state and build output (spec 110 B-1; the
# observatory's ignores at their new paths). Captured observations of
# ~/.claude are never committed: they may reference private activity.
members/data/
members/node_modules/
members/web/dist/
members/dist/
*.log
*.db
*.db-wal
*.db-shm
.derived/**/build-meta.json
.derived/attestation/
