# Rendered by Statecraft from profile github-actions-rust revision {{sc:profile.revision}},
# because this repository had no CODEOWNERS. Branch protection should require
# code-owner review, so a change to the gate that judges pull requests needs
# a review its author cannot give (decision S-3).
{{sc:codeowners.lines}}
