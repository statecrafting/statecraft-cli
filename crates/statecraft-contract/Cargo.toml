[package]
name = "statecraft-contract"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "The Statecraft member contract: the manifest, exit codes, envelope and driver seam every member and the umbrella agree on."
repository = "https://github.com/statecrafting/statecraft-cli"
publish = false

[package.metadata.spec-spine]
spec = "111-contract-crate"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
